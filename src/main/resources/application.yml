server:
  port: @server.port@
management:
  security:
    flag: false
  metrics:
    export:
      statsd:
        flavor: etsy
spring:
  application:
    name: @project.name@
info:
  version: @project.version@
  stage: dev
preparing:
  delay: 5000
operations:
  lifetime: 60000
